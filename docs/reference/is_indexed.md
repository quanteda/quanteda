# Check if a glob pattern is indexed by index_types

Internal function for `select_types` to check if a glob pattern is
indexed by `index_types`.

## Usage

``` r
is_indexed(pattern)
```

## Arguments

- pattern:

  a glob pattern to be tested
