# Function extending base::attributes()

Function extending base::attributes()

## Usage

``` r
attributes(x, overwrite = TRUE) <- value
```

## Arguments

- x:

  an object

- overwrite:

  if `TRUE`, overwrite old attributes

- value:

  new attributes
