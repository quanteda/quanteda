# Internal function to convert a list to a dictionary

A dictionary is internally a list of list to keys and values to coexist
in the same level.

## Usage

``` r
list2dictionary(dict)
```

## Arguments

- dict:

  list of object
